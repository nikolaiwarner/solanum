import React, { ReactElement, useCallback, useEffect, useRef, useState } from 'react'

import { secondsToTime } from '../Lib/Time'

export default function SettingsScreen(): ReactElement {
  return <div className="settings-screen"></div>
}
